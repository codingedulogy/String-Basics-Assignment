#include<bits/stdc++.h>
using namespace std;
bool comparator(string a,string b)
{
    return a<b;
}
int main()
{
    int n=5;
    string arr[5];
    for(int i=0; i<n; i++)
    {
        cin>>arr[i];
    }
    sort(arr,arr+n,comparator);
       for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<" ";
    }
}
